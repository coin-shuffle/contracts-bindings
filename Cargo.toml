[package]
name = "coin-shuffle-contracts-bindings"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
ethers = "1"

[build-dependencies]
eyre = "0.6.8"
ethers = { version = "1.0.0", features = ["abigen", "ethers-solc"] }
serde_json = "1.0"
